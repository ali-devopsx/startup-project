name: CI Build Django APP
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker
        run: docker --version

      - name: Build Docker image
        run: docker build -t my_ci_django .

# test
      - name: Run Container and test
        run: |
         docker run -d --name test_containet -p 8000:8000 my_ci_django
         sleep 10
         docker exec test_container python manage.py migrate

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker build image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/django-app .
     
      - name: Docker Push Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/django-app
